---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &appname houndarr
  namespace: flux-system
spec:
  targetNamespace: downloads
  components:
    - ../../../../components/volsync
  commonMetadata:
    labels:
      app.kubernetes.io/name: *appname
  interval: 30m
  timeout: 5m
  path: "./k8s/apps/downloads/houndarr/app"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  dependsOn:
    - name: cluster-apps-rook-ceph-cluster
    - name: cluster-apps-volsync-app
    - name: radarr
    - name: sonarr
  postBuild:
    substitute:
      APP: *appname
      VOLSYNC_CLAIM: *appname
      VOLSYNC_CAPACITY: 2Gi
