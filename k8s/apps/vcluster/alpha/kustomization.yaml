apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: test-cluster-1
resources:
  - release.yaml
configMapGenerator:
  - name: values-test-cluster-1
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml