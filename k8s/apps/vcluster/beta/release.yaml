apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-cluster-2
spec:
  releaseName: test-cluster-2
  chart:
    spec:
      # renovate: registryUrl=https://charts.loft.sh
      chart: vcluster
      version: 0.16.2
      sourceRef:
        kind: HelmRepository
        name: loft
        namespace: flux-system
  interval: 5m0s
  valuesFrom:
    - kind: ConfigMap
      name: values-test-cluster-2
  
