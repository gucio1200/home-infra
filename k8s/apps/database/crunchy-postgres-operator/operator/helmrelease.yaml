---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crunchy-postgres-operator
  namespace: database
spec:
  interval: 30m
  chart:
    spec:
      chart: pgo
      version: 5.7.0
      sourceRef:
        kind: HelmRepository
        name: crunchydata
        namespace: flux-system
      interval: 5m
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
